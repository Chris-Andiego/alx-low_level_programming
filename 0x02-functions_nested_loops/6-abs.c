#include "main.h"

/**
 * _abs - check main
 * @r : input
 * Description: returns value of a number
 * Return: absolute value or number
 */

int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (r * -1);
}
